<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PokemonList" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/primary_color_dark</item>
        <item name="colorOnPrimary">@color/text_color_primary</item>
        <item name="colorControlNormal">@color/text_color_primary</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorSecondaryVariant">@color/secondary_color_dark</item>
        <item name="colorOnSecondary">@color/text_color_primary</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/accent_color</item>
        <item name="android:textColor">@color/text_color_primary</item>
        <item name="android:fontFamily">@font/mulish_regular</item>
        <item name="android:toolbarStyle">@style/ToolBarStyle</item>
        <item name="android:windowBackground">@drawable/splash_screen_background</item>

        <!-- TextColors -->
        <item name="android:textColorPrimary">@color/text_color_primary</item>
        <item name="android:textColorHint">@color/text_color_hint</item>

        <!-- CardView's shadow depth -->
        <item name="android:ambientShadowAlpha">0.02</item>
        <item name="android:spotShadowAlpha">0.4</item>

    </style>

    <style name="Theme.Activity" parent="Theme.PokemonList">
        <item name="android:windowBackground">@drawable/drawable_app_background</item>
    </style>

    <style name="Theme.NoBackgroundActivity" parent="Theme.PokemonList">
        <item name="android:windowBackground">@color/transparent</item>
    </style>
</resources>