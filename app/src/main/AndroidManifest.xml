<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nehak.pokemonlist">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".PokemonApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustPan">
        <activity
            android:name=".ui.pokemonList.PokemonListActivity"
            android:exported="true"
            android:theme="@style/Theme.PokemonList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.pokemonDetails.PokemonDetailsActivity"
            android:theme="@style/Theme.Activity" />
        <activity
            android:name=".ui.pokemonSearch.PokemonSearchActivity"
            android:theme="@style/Theme.Activity" />
    </application>

</manifest>